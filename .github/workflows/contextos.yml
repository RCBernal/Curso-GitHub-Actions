name: Aprendiendo contextos en GitHub Actions
run-name: Estoy aprendiendo Contextos de GitHub Actions

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: mostrar info del repo
        run: |
          echo: "RAMA=${{ github.ref_name }}" 
          echo: "REFERENCIA=${{ github.ref }}" 
          echo: "REPOSITORIO=${{ github.repository }}" 
          echo: "USERNAME=${{github.actor}}" 
          echo: "ID=${{github.repository_id	}}"
  not-main:
     if: ${{ github.ref != 'refs/heads/main'}}
     runs-on: ubuntu-latest
     steps:
       - name: mostrar rama
         run: echo "Esta no es la rama Main, es la rama ${{ github.ref_name }}"
  
      
